# --- secrets & local operator notes ---
secrets.env
*.env
!secrets.env.example
secrets/
summary.md

# --- kubeconfigs / cluster auth ---
kubeconfigs/
*.kubeconfig

# --- Terraform (tokens, state, local provider cache) ---
**/secrets.auto.tfvars
**/*.auto.tfvars
!**/terraform.tfvars
**/*.tfstate
**/*.tfstate.*
**/*.tfplan
**/tfplan
**/.terraform/
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- Talos bootstrap artifacts (talosconfig, machine configs, secrets) ---
generated/

# --- deploy-generated Helm/Ansible overlays ---
scripts/generated/

# --- Ansible inventories (hosts + group_vars; may contain OAuth / tokens) ---
ansible/inventories/

# --- editor / OS junk ---
.DS_Store
*.swp
*~
.idea/
.vscode/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
