---
# Tailscale operator — reuses kubeconfigs/<ENV_ID>.kubeconfig when present.

- name: Tailscale Kubernetes Operator (cluster pods only)
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - role: tailscale_operator
